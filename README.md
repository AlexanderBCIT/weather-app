A basic weather app. Built in VS Code using Angular and PrimeNG for the front-end. 
This was just a small project for the fun of it. 
Used OpenWeather API url to run it locally and to see if it works. 

Start app by running ng-serve
